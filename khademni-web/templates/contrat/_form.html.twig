{{ form_start(form) }}
    {{ form_errors(form) }}

    {% set client_hidden = 'hidden' in form.clientId.vars.block_prefixes %}
    {% set freelancer_hidden = 'hidden' in form.freelancerId.vars.block_prefixes %}

    {% if client_hidden %}
        {{ form_widget(form.clientId) }}
        {{ form_errors(form.clientId) }}
    {% endif %}

    {% if freelancer_hidden %}
        {{ form_widget(form.freelancerId) }}
        {{ form_errors(form.freelancerId) }}
    {% endif %}

    {% if not client_hidden or not freelancer_hidden %}
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
            {% if not client_hidden %}
                <div>
                    {{ form_label(form.clientId) }}
                    {{ form_widget(form.clientId) }}
                    {{ form_errors(form.clientId) }}
                </div>
            {% endif %}

            {% if not freelancer_hidden %}
                <div>
                    {{ form_label(form.freelancerId) }}
                    {{ form_widget(form.freelancerId) }}
                    {{ form_errors(form.freelancerId) }}
                </div>
            {% endif %}
        </div>
    {% endif %}
    
    <div style="margin-top: 15px;">
        {{ form_label(form.titre) }}
        {{ form_widget(form.titre) }}
        {{ form_errors(form.titre) }}
    </div>
    
    <div style="margin-top: 15px;">
        {{ form_label(form.description) }}
        {{ form_widget(form.description) }}
        {{ form_errors(form.description) }}
    </div>
    
    <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 20px; margin-top: 15px;">
        <div>
            {{ form_label(form.prix) }}
            {{ form_widget(form.prix) }}
            {{ form_errors(form.prix) }}
        </div>
        <div>
            {{ form_label(form.dateContrat) }}
            {{ form_widget(form.dateContrat) }}
            {{ form_errors(form.dateContrat) }}
        </div>
        <div>
            {{ form_label(form.numTelephone) }}
            {{ form_widget(form.numTelephone) }}
            {{ form_errors(form.numTelephone) }}
        </div>
    </div>
    
    <div style="margin-top: 30px; text-align: right;">
        <button type="submit" class="button-primary">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
