{% extends 'base.html.twig' %}

{% block title %}{{ module.title }} | 5ademni.tn{% endblock %}

{% block body %}
    <div class="page-shell">
        {% include 'partials/_header.html.twig' %}

        <main class="page-content">
            <section class="placeholder-card">
                <p class="eyebrow">Module Placeholder</p>
                <h1>{{ module.title }}</h1>
                <p class="placeholder-copy">{{ module.description }}</p>
                <a class="primary-button primary-button-inline" href="{{ path('app_profile') }}">Back to Profile</a>
            </section>
        </main>
    </div>
{% endblock %}
