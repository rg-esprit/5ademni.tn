# ── Face Recognition Backend — Python 3.13 + conda base (ARM64 / Apple Silicon) ─
#
# Backend now uses face_recognition (dlib ResNet-34) — NO TensorFlow/DeepFace needed.
# TensorFlow 2.20 hangs on Python 3.13 ARM64; dlib via conda-forge works fine.
#
# One-time setup:
#   conda install -c conda-forge dlib -y
#   pip install face_recognition fastapi "uvicorn[standard]" \
#               python-multipart mysql-connector-python opencv-python
#
# Start (from repo root  /path/to/5ademni.tn):
#   /opt/anaconda3/bin/python -m uvicorn face_backend.main:app \
#       --host 0.0.0.0 --port 5003 --reload
#
# Port 5000  → macOS AirPlay Receiver (permanent conflict)
# Port 5001/5002 → lingering CLOSED sockets from previous runs
# Port 5003  → clean, confirmed working
# ─────────────────────────────────────────────────────────────────────────────
face_recognition>=1.3.0         # wraps dlib ResNet-34 → 128-dim face descriptors
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
mysql-connector-python>=8.3.0
numpy>=1.26.0
opencv-python>=4.8.0            # webcam capture + image decode
